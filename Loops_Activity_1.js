for (i=0; i<=15; i++) {				// created a loop that iterates from 0 to 15
	if (i%2 == 0) {				// check if the remainder of the given number's division by 2 is 0
		console.log(`${i} is even`);	// if true output that the given number is even
	} else {				
		console.log(`${i} is odd`);	// if false output that the given number is odd
	}
	
}


