x = int(input("Input the first number: "))  #create a variable that will take string input and turn it into an integer
y = int(input("Input the second number: ")) #create a second variable that takes input and turns it into an integer
z = str(int(x/y)) # create a variable that will store the value which is the result of the division of x by y, turn it into an integer and then into a string
print("The division of the first number and the second number is: "+z)   # output a string and the value of z variable concatenated to it
